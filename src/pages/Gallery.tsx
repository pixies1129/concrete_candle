import { EmptyState } from '../components/EmptyState'

export function Gallery() {
  return (
    <EmptyState icon="▣" title="내 이미지">
      생성한 이미지를 모아보는 기능은 곧 제공됩니다.
      <br />
      지금은 생성 결과 화면에서 바로 다운로드해주세요.
    </EmptyState>
  )
}
